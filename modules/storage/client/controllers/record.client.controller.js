'use strict';

// Create the 'chat' controller
angular.module('storage').controller('RecordController', ['$stateParams', 'StorageService',
    function ($stateParams, StorageService) {

        var vm = this;
        vm.categories = [];

    }
]);
